# ─── Bootstrap secrets — 永不入库(R8 红线) ───
config/.secret_master.key
config/secrets/
config/license.lic
*.pem
*.key

# ─── 运行时本地状态 ───
data/
logs/
tmp/
backups/
.venv/

# ─── Python ───
__pycache__/
*.py[cod]
*.egg-info/
*.so
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ─── uv ───
# 注意:uv.lock 必须进 git(portable 可复现构建依赖此 lockfile)

# ─── Win10 离线部署包构建产物(zip / wheels / PG 二进制 / 缓存,一律不进 git) ───
dist-bundle/

# ─── 前端 ───
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/test-results/
frontend/playwright-report/
frontend/.playwright/

# ─── IDE / OS ───
.DS_Store
.idea/
.vscode/
*.swp

# ─── MCP / 临时调试产物 ───
.playwright-mcp/
.worktrees/
# 根目录 PNG(MCP 截图等);frontend/src/assets/*.png 不受影响
/*.png
